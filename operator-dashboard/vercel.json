{
  "buildCommand": "npm install && npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/operator/(.*)",
      "destination": "/operator/index.html"
    }
  ]
}

